const heroes = ["Thor", "IronMan", "SpiderMan"];
const dc_heroes = ["SuperMan", "Flash", "Batman"];

// heroes.push(dc_heroes);

// console.log(heroes);
// console.log(heroes[3][1]);

// const allHeroes = heroes.concat(dc_heroes);
// this gives a new array which contains both the elements without inserting one array in another

// const all_new_heros = [...heroes, ...dc_heroes];
// This method is prefered to concatinate the arrays as you can concat how much ever you need .

// .flat methood converts a nested array into a single array

// console.log(all_new_heros);

// refer to the original for this
